
===================Queue Commands =========================================
Define QLocal(queuename)
Define QLocal(queuename) Usage(Xmitq)

Define QAlias(queuename) Target(Targetqueuename)

Define QRemote(queueNmae) RName(TargetQueueName) RQMName(TargetQMGRName) Xmitq(XmitqName)

Define QModel(queuename) Deftype(PermDyn)

Display QLocal(queuename)
Display Qstatus(queuename)
Display QLocal(queuename) Usage
Display QAlias(queuename)
Display QRemote(queuename) All
Display QModel(queuename)

Alter QLocal(queuename) Maxdepth(10000) Maxmsgl(1024(bytes))
Alter QRemote(queuename) Rname(TargetQueueName) RQMName(TargetQMGRName) Xmitq(XmitqName)
Alter QModel(queuename) Deftype(PermDyn)
Alter QAlias(queueName) Target(a)

Delete QLocal(queuename)
Delete QAlias(queueNmae)
Delete QRemote(queuename)
Delete QModel(queuename)

Clear QLocal(queuename)
